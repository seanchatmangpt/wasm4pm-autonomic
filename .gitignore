/target
**/target/
**/*.rs.bk
*.json
.DS_Store

# Accidental home-path copies (must never be tracked)
/Users/
.wreckit/

# Ralph-generated planning artifacts — never commit
/plans/
/ideas/
/IDEAS.md

# Portfolio proof artifacts — receipts, envelopes, and closure docs are evidence
!.portfolio/
!.portfolio/receipts/
!.portfolio/receipts/*.json
!.portfolio/obligations/
!.portfolio/obligations/*.md
# Run inputs (plan corpora) stay ignored — only signed outputs matter
.portfolio/runs/
