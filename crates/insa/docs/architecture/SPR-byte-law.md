# SPR: INSA Byte Law and Maximum Rust Speed

## Core Thesis
**Speed is not an optimization; speed is what makes instinct possible.**
Performance is Semantic Commitment Earlier in the Pipeline. Less runtime interpretation equals Lower branch/allocation/indirection cost.

INSA is not a stable Rust library. It is a maximum-speed Rust autonomic substrate.

## The Byte Law: Byte-Width Semantic Multiplexing
* **8 is not arbitrary bounded cognition.** 8 is byte-width semantic multiplexing.
* **A single byte can carry 256 activation surfaces.**
* **The byte is not compression after design; the byte is the design space.**
* **INST8:** u8 power-set semantics, not an enum choice. Activation can be many; selected instinct must be one.
* **KAPPA8:** u8 collapse attribution, not narrative explanation.
* **COG8:** max 8-field closure.
* **POWL8:** byte-sized process motion.
* **CONSTRUCT8:** max 8-entry mutation. Widening means refactor.
* **POWL64:** compact proof spine, downstream of hot motion.

## The Decomposition Law
**Need9 means decompose.**
If a semantic surface needs more than one byte, first decompose it into byte-aligned fields before widening it.
Need9 means the semantic surface has crossed one-byte closure and must become composition, sequencing, hierarchy, or another byte lane.

**Do not widen first. Factor first.**

## Rust-Specific Execution Posture
* **Lawful Reference Path First.** Fastest equivalent Rust path wins.
* **No Stable/Nightly Purity Test.** Admitted vs. Unadmitted is the boundary. Nightly is permitted when it produces measurable lawful speed.
* **Features:** u8 activation bytes, one-hot selected instinct, 256-entry lookup tables, bitwise AND/OR/XOR, branchless predicates, cache-dense arrays, SIMD batchability.
* **Unsafe:** Allowed only when isolated, benchmark-proven, sanitized, fuzzed, wrapped in safe APIs, and documented with exact invariants.

## The Hierarchy of Truth
1. ReferenceLawPath(x) = FastPath(x) (Semantic equivalence is absolute).
2. Proof witnesses hot motion; proof does not govern hot motion.
3. Every admitted path must survive Truthforge adversarial testing.
