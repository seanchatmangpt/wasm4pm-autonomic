# Evolution Trace — From ccog / ainst to INSA

The conversation did not start as “let’s build a CLI framework” or “let’s make templates.”
It started as a much deeper correction:

> ccog/ainst had grown beyond exploration. INSA emerged as the exploit-phase extraction of what survived.

The actual evolution was:
ccog / ainst exploration
  ↓
adversarial evidence demand
  ↓
extract law, not code
  ↓
INSA as new project
  ↓
byte-speed instinct substrate
  ↓
layout/admission doctrine
  ↓
doctor / wizard / telco operational lifecycle
  ↓
unrdf + templates as manufacturing line
  ↓
AGENTS.md + pattern language
  ↓
Vision 2030 production retrospective

The final thing we built is not just "Instinctual Autonomics."
It is:
> A production system for manufacturing, admitting, operating, and replaying byte-speed instinct.

## The Canonical Architecture Stack

### Kernel layer
O*, COG8, KAPPA8, Family8, INST8, InstinctResolution, POWL8, CONSTRUCT8, POWL64

### Admission layer
ReferenceLawPath, CandidateFastPath, Truthforge, layout gates, wire gates, golden fixtures, benchmark gates, replay gates, de-admission

### Lifecycle layer
doctor, wizard, telco, replay, pack, bench, release

### Manufacturing layer
unrdf sync, just stations, templates, queries, generated crates, schemas, OpenAPI, docs, MCP descriptors

### Communication/projection layer
MCP, A2A, HITL, ProjectionIntent, CONSTRUCT8 reentry, telco provision/bind/route/test/trace/fault/restore, control/data/proof plane separation

### Agent governance layer
AGENTS.md, local AGENTS.md, evidence-first completion, no fake done
